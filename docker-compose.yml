version: '3'

services:
  web-back:
    container_name: crunchy_api
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "127.0.0.1:9990:80"

networks:
  default:
    external:
      name: crunchy_net